# 知识树与每日灵感浏览器验收

日期：2026-09-09
分支：`feat/notes-foundation`

## 验收结论

知识树与“每日灵感”已成为笔记工作台顶部两个并列入口。全局不显示加号；知识树只在当前选中的“笔记库”或文件夹旁显示 `＋`。手机选择或新建文件夹/笔记后自动关闭左侧抽屉，并在右侧显示完整路径与对应内容；“每日灵感”使用独立本地日期记录，不进入用户知识树。

## 数据与保存

- Dexie v8 在保留 v1-v7 表结构的基础上新增 `dailyInspirations`，旧任务、周期、文件夹和笔记迁移测试通过。
- 日期键来自本地日历字段 `YYYY-MM-DD`，覆盖深夜本地时间、闰日、非法日期、0001 与 9999 年边界，不使用 UTC 转换。
- 未书写的日期不会创建记录；清空已有内容会删除记录及日历圆点。
- 320ms 自动保存、中文输入法组合输入、失败草稿保留、重试、修订冲突、卸载后的迟到保存保护均有组件回归。

## 浏览器流程

Chromium 生产构建验收 9/9 通过，覆盖：

- 组合输入期间不提前保存；组合结束后自动保存，刷新页面仍显示原日期与正文。
- 上一月、下一月、年份与月份直达，以及 390px 手机横向滑月。
- 有内容日期显示圆点；清空并刷新后正文为空且圆点消失。
- 卡片没有标题、日期、占位文字、正常保存状态、操作栏或回形针；正文无内部滚动条并随长内容增高。
- 知识树可在固定“笔记库”下建立任意数量的根文件夹或根笔记，并继续无限细分；只有当前根或文件夹显示 `＋`。
- 手机从节点旁 `＋` 建立文件夹和子笔记，新建行显示完整父路径，成功后抽屉关闭并打开正确右侧页面。
- 普通笔记只有一个直接书写页面，不显示编辑/阅读/分栏切换及二级标题、加粗、列表、引用、行内代码按钮。
- 回收站文件夹子树仍可浏览和恢复；普通笔记失败草稿与多页面修订冲突流程未回归。

## 响应式与无障碍

已验证 390×695、402×695、768×1024、834×1112、1024×900、1366×900：

- 页面无水平溢出。
- 日历日期行不重叠；当月网格只显示当月日期数字，前后月位置留空；日期、年月、前后月、“今天”和主要知识树入口均至少 44×44px。
- 390/402px 下日历高 328px，纸张卡片最低约 446.8px，书写卡片高于日历。
- 手机显示目录抽屉；平板与桌面保留常驻左侧目录。
- `prefers-reduced-motion` 路径正常；每日灵感区域没有 serious / critical axe 违规。

## 截图

- [402×695 手机每日灵感](assets/notes-daily-inspiration-402.png)
- [1024×900 桌面每日灵感](assets/notes-daily-inspiration-1024.png)

## 预览

缓存隔离的局域网预览：`http://192.168.31.125:4179/notes?preview=6`

新端口用于避开 4178 的旧版 Service Worker。手机与电脑需连接同一可信 Wi-Fi；不同地址或设备的 IndexedDB 数据不会自动同步。

该地址与旧 4176/4177 端口使用不同来源，不会继续命中旧版 Service Worker。手机与电脑需连接同一可信 Wi-Fi；不同地址或设备的 IndexedDB 数据不会自动同步。

## 当前阶段边界

本阶段完成知识树、完整路径、手机目录抽屉和每日灵感。所见即所得笔记编辑、搜索、真实 PDF 导出及回收站永久删除继续作为已确认的后续子项目；公式、录音、AI 与同步不进入首版。
